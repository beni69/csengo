{
  "name": "csengo-frontend",
  "scripts": {
    "build": "esbuild bundle.js --bundle --minify --platform=browser --sourcemap --outdir=../static",
    "watch": "watchexec 'pnpm build'"
  },
  "dependencies": {
    "@picocss/pico": "^1.5.10",
    "htmx.org": "^1.9.4"
  },
  "devDependencies": {
    "esbuild": "^0.19.2"
  }
}
